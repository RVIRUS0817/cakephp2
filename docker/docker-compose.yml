
  nginx:
    build: ./nginx/
    ports:
      - 8080:80
    links:
      - phpfpm
    volumes:
      - ../cakephp2/cakephp:/var/www/html/cakephp2

  phpfpm:
    build: ./phpfpm/
    links:
      - mysql
    volumes:
      - ../cakephp2/cakephp:/var/www/html/cakephp2

  mysql:
    image: mysql:5.7
    environment:
      MYSQL_DATABASE: my_app
      MYSQL_ROOT_PASSWORD: test
